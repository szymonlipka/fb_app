.column-center
  %h5
    Zaproszenia:
  - @user.invitations.reverse.each do |invite|
    %a{href: "/dashboards/2/accept_invitation?invite=#{invite.id}", method: 'post', class: 'btn btn-success' }
      Accept
    %a{href: "/dashboards/2/decline_invitation?invite=#{invite.id}", method: 'delete', class: 'btn btn-danger' }
      Decline
    =invite.group_name
    - unless invite.group_name
      #{invite.inviter_username} wants to add you to his friends
    %br