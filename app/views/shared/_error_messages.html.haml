- if @group.errors.any?
  #error_explanation
    .alert.alert-danger
      The group contains #{pluralize(@group.errors.count, "error")}.
    %ul
      - @group.errors.full_messages.each do |msg|
        %li= msg
